# Romanization backends and the multi-provider scraping engine
pykakasi>=2.2
pypinyin>=0.49
korean-romanizer>=0.25
anyascii>=0.3
syncedlyrics>=0.7
# Windows only: provides the readline API used for Tab completion and prefills
pyreadline3>=3.4; sys_platform == "win32"
